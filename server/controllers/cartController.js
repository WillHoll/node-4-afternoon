const swag = require('../models/swag');

module.exports = {
    add: (req, res) => {
        const {id} = req.params;
        if (req.session.user.cart.find)
    },
    delete: (req, res) => {

    },
    checkout: (req, res) => {

    }
}